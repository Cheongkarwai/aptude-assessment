Backend

1. Open project in your IDE
2. run mvn clean install to install all the dependencies
3. run mvn spring-boot:run to start up your application


Frontend
1. Open project in your IDE
2. run npm install to install all the necessary node modules
3. run ng serve command to start up Angular application